---
sidebar_label: "launchers.events.provider.triggerEvent"
---

Overwolf APIs / overwolf / overwolf / games / launchers / events / provider / triggerEvent

```ts
function triggerEvent(
  launcherClassId: number,
  feature: string,
  name: string,
  data?: any,
  callback?: CallbackFunction<Result>,
): void;
```

Triggers a synthetic game event for the specified launcher.

## Parameters

| Parameter         | Type                                                                                                                                                                    |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `launcherClassId` | `number`                                                                                                                                                                |
| `feature`         | `string`                                                                                                                                                                |
| `name`            | `string`                                                                                                                                                                |
| `data?`           | `any`                                                                                                                                                                   |
| `callback?`       | `CallbackFunction`\<`Result`\> |

## Returns

`void`
