---
sidebar_label: "launchers.events.provider.setSupportedFeatures"
---

Overwolf APIs / overwolf / overwolf / games / launchers / events / provider / setSupportedFeatures

```ts
function setSupportedFeatures(
  launcherClassId: number,
  features: string[],
  callback?: CallbackFunction<Result>,
): void;
```

Declares the set of features that this launcher event provider supports.

## Parameters

| Parameter         | Type                                                                                                                                                                    |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `launcherClassId` | `number`                                                                                                                                                                |
| `features`        | `string`[]                                                                                                                                                              |
| `callback?`       | `CallbackFunction`\<`Result`\> |

## Returns

`void`
