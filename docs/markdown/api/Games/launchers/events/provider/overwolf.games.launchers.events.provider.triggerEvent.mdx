Overwolf APIs / overwolf.benchmarking / overwolf / games / launchers / events / provider / triggerEvent

```ts
function triggerEvent(
  launcherClassId: number,
  feature: string,
  name: string,
  data?: any,
  callback?: CallbackFunction<Result>,
): void;
```

## Parameters

| Parameter         | Type                                                                                                                                                                    |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `launcherClassId` | `number`                                                                                                                                                                |
| `feature`         | `string`                                                                                                                                                                |
| `name`            | `string`                                                                                                                                                                |
| `data?`           | `any`                                                                                                                                                                   |
| `callback?`       | `CallbackFunction`\&lt;`Result`\&gt; |

## Returns

`void`
