Overwolf APIs / overwolf.benchmarking / overwolf / games / launchers / events / setRequiredFeatures

```ts
function setRequiredFeatures(
  launcherClassId: number,
  features: string[],
  callback: CallbackFunction<SetRequiredFeaturesResult>,
): void;
```

Sets the required features from the provider.

## Parameters

| Parameter         | Type                                                                                                                                                                            | Description                            |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| `launcherClassId` | `number`                                                                                                                                                                        | -                                      |
| `features`        | `string`[]                                                                                                                                                                      | A string array of features to utilize. |
| `callback`        | `CallbackFunction`\&lt;`SetRequiredFeaturesResult`\&gt; | Called with success or failure state.  |

## Returns

`void`
