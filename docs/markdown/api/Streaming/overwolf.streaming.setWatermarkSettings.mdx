Overwolf APIs / overwolf.benchmarking / overwolf / streaming / setWatermarkSettings

```ts
function setWatermarkSettings(
  settings: WatermarkSettings,
  callback?: CallbackFunction<Result>,
): void;
```

Sets the watermark settings.

## Parameters

| Parameter   | Type                                                                                                                                | Description                                                      |
| ----------- | ----------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| `settings`  | `WatermarkSettings`                                                                          | The new watermark settings.                                      |
| `callback?` | `CallbackFunction`\&lt;`Result`\&gt; | A callback to call when done setting the new watermark settings. |

## Returns

`void`
