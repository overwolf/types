---
sidebar_label: "getCapabilities"
---

Overwolf APIs / overwolf / overwolf / streaming / getCapabilities

```ts
function getCapabilities(
  callback: CallbackFunction<StreamingCapabilities>,
): void;
```

Returns the streaming capabilities of the current system.

## Parameters

| Parameter  | Type                                                                                                                                                        | Description                                      |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ |
| `callback` | `CallbackFunction`\<`StreamingCapabilities`\> | Called with the system's streaming capabilities. |

## Returns

`void`
