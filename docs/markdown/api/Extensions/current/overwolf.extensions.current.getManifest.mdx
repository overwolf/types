---
sidebar_label: "current.getManifest"
---

Overwolf APIs / overwolf / overwolf / extensions / current / getManifest

```ts
function getManifest(callback: CallbackFunction<GetManifestResult>): void;
```

Returns the current extension's manifest object.

## Parameters

| Parameter  | Type                                                                                                                                                            | Description                               |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- |
| `callback` | `CallbackFunction`\<`GetManifestResult`\> | A function called with the manifest data. |

## Returns

`void`
