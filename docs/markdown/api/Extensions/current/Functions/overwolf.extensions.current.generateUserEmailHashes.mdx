---
sidebar_label: "current.generateUserEmailHashes"
---

Overwolf APIs / overwolf / overwolf / extensions / current / generateUserEmailHashes

```ts
function generateUserEmailHashes(
  email: string,
  callback: CallbackFunction<Result>,
): void;
```

Generates SHA1, SHA256, and MD5 hashes from the given email address.

## Parameters

| Parameter  | Type                                                                                                                                            |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| `email`    | `string`                                                                                                                                        |
| `callback` | `CallbackFunction`\<`Result`\> |

## Returns

`void`
