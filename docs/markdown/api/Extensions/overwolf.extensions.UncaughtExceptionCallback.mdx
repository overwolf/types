Overwolf APIs / overwolf.benchmarking / overwolf / extensions / UncaughtExceptionCallback

```ts
type UncaughtExceptionCallback = (
  message: string,
  functionName: string,
  scriptName: string,
) => void;
```

The following types are related to the |onUncaughtException| event - which
is a different than the usual events.

## Parameters

| Parameter      | Type     |
| -------------- | -------- |
| `message`      | `string` |
| `functionName` | `string` |
| `scriptName`   | `string` |

## Returns

`void`
