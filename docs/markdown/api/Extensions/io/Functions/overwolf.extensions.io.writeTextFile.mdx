---
sidebar_label: "io.writeTextFile"
---

Overwolf APIs / overwolf / overwolf / extensions / io / writeTextFile

```ts
function writeTextFile(
  space: StorageSpace,
  filePath: string,
  content: string,
  callback: CallbackFunction<Result>,
): void;
```

Writes text content to the file at `filePath` within the storage space, creating it if it does not exist.

## Parameters

| Parameter  | Type                                                                                                                                            |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| `space`    | `StorageSpace`                                                                             |
| `filePath` | `string`                                                                                                                                        |
| `content`  | `string`                                                                                                                                        |
| `callback` | `CallbackFunction`\<`Result`\> |

## Returns

`void`
