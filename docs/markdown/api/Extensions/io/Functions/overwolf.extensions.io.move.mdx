---
sidebar_label: "io.move"
---

Overwolf APIs / overwolf / overwolf / extensions / io / move

```ts
function move(
  space: StorageSpace,
  source: string,
  destination: string,
  callback: CallbackFunction<Result>,
): void;
```

Moves a file or directory from `source` to `destination` within the storage space.

## Parameters

| Parameter     | Type                                                                                                                                            |
| ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| `space`       | `StorageSpace`                                                                             |
| `source`      | `string`                                                                                                                                        |
| `destination` | `string`                                                                                                                                        |
| `callback`    | `CallbackFunction`\<`Result`\> |

## Returns

`void`
