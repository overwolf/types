---
sidebar_label: "io.getStoragePath"
---

Overwolf APIs / overwolf / overwolf / extensions / io / getStoragePath

```ts
function getStoragePath(
  space: StorageSpace,
  callback: CallbackFunction<GetStoragePathResult>,
): void;
```

Returns the absolute file system path for the given storage space.

## Parameters

| Parameter  | Type                                                                                                                  |
| ---------- | --------------------------------------------------------------------------------------------------------------------- |
| `space`    | `StorageSpace`                                                   |
| `callback` | `CallbackFunction`\<`GetStoragePathResult`\> |

## Returns

`void`
