---
sidebar_label: "io.createDirectory"
---

Overwolf APIs / overwolf / overwolf / extensions / io / createDirectory

```ts
function createDirectory(
  space: StorageSpace,
  path: string,
  callback: CallbackFunction<Result>,
): void;
```

Creates a new directory at the given path within the specified storage space.

## Parameters

| Parameter  | Type                                                                                                                                            |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| `space`    | `StorageSpace`                                                                             |
| `path`     | `string`                                                                                                                                        |
| `callback` | `CallbackFunction`\<`Result`\> |

## Returns

`void`
