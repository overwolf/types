---
sidebar_label: "io.exist"
---

Overwolf APIs / overwolf / overwolf / extensions / io / exist

```ts
function exist(
  space: StorageSpace,
  path: string,
  callback: CallbackFunction<ExistResult>,
): void;
```

Checks whether a file or directory exists at the given path in the storage space.

## Parameters

| Parameter  | Type                                                                                                         |
| ---------- | ------------------------------------------------------------------------------------------------------------ |
| `space`    | `StorageSpace`                                          |
| `path`     | `string`                                                                                                     |
| `callback` | `CallbackFunction`\<`ExistResult`\> |

## Returns

`void`
