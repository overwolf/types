Overwolf APIs / overwolf.benchmarking / overwolf / profile / subscriptions / inapp / hide

```ts
function hide(callback: CallbackFunction<Result>): void;
```

Hide the current active in-app subscription modal window.

## Parameters

| Parameter  | Type                                                                                                                                                        | Description                                                              |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| `callback` | `CallbackFunction`\&lt;`Result`\&gt; | A callback function which will be called with the status of the request. |

## Returns

`void`
