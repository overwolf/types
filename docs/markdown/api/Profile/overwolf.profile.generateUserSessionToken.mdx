Overwolf APIs / overwolf.benchmarking / overwolf / profile / generateUserSessionToken

```ts
function generateUserSessionToken(
  callback: CallbackFunction<GenerateUserSessionTokenResult>,
): void;
```

## Parameters

| Parameter  | Type                                                                                                                                                                          |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `callback` | `CallbackFunction`\&lt;`GenerateUserSessionTokenResult`\&gt; |

## Returns

`void`
