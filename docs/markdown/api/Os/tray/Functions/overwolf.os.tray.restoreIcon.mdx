---
sidebar_label: "tray.restoreIcon"
---

Overwolf APIs / overwolf / overwolf / os / tray / restoreIcon

```ts
function restoreIcon(callback: CallbackFunction<Result>): void;
```

Restores the tray icon to the extension's default icon.

## Parameters

| Parameter  | Type                                                                                                                                            | Description             |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| `callback` | `CallbackFunction`\<`Result`\> | Called with the result. |

## Returns

`void`
