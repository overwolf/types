---
sidebar_label: "hotkeys.getAllApps"
---

Overwolf APIs / overwolf / overwolf / settings / hotkeys / getAllApps

```ts
function getAllApps(
  callback: CallbackFunction<GetAllAssignedHotkeysResult>,
): void;
```

Returns the hotkeys assigned for all installed extensions + the platform, in all the games.

## Parameters

| Parameter  | Type                                                                                                                                                                          | Description                                                                       |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| `callback` | `CallbackFunction`\<`GetAllAssignedHotkeysResult`\> | Called with a map of all app hotkey assignments alongside platform-level hotkeys. |

## Returns

`void`
