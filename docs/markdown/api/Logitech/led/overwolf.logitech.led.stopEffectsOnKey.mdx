Overwolf APIs / overwolf.benchmarking / overwolf / logitech / led / stopEffectsOnKey

```ts
function stopEffectsOnKey(
  keyName: KeyboardNames,
  callback: CallbackFunction<Result>,
): void;
```

Stops ongoing pulse / flash effects on a specific key.

## Parameters

| Parameter  | Type                                                                                                                                            | Description                                |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| `keyName`  | `KeyboardNames`                                                                           | The key name. For a list of key names see  |
| `callback` | `CallbackFunction`\&lt;`Result`\&gt; | A callback with the result of the request. |

## Returns

`void`
