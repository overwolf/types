---
sidebar_label: "discord.shareEx"
---

Overwolf APIs / overwolf / overwolf / social / discord / shareEx

```ts
function shareEx(
  discordShareParams: ShareParameters,
  resultCallback: CallbackFunction<SocialShareResult>,
  progressCallback: CallbackFunction<SocialShareProgress>,
): void;
```

Shares a media file to a Discord channel with separate result and progress callbacks.

## Parameters

| Parameter            | Type                                                                                                                                                          | Description                                                                  |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| `discordShareParams` | `ShareParameters`                                                                                                        | The share parameters.                                                        |
| `resultCallback`     | `CallbackFunction`\<`SocialShareResult`\>     | Called with the final share result, including the URL of the shared content. |
| `progressCallback`   | `CallbackFunction`\<`SocialShareProgress`\> | Called periodically with upload progress updates.                            |

## Returns

`void`
