---
sidebar_label: "discord.getDisabledServices"
---

Overwolf APIs / overwolf.social.discord / getDisabledServices

```ts
function getDisabledServices(
  callback: CallbackFunction<GetDisabledServicesResult<void>>,
): void;
```

Checks which of the supported sharing services are disabled or enabled.

## Parameters

| Parameter  | Type                                                                                                               | Description                         |
| ---------- | ------------------------------------------------------------------------------------------------------------------ | ----------------------------------- |
| `callback` | `CallbackFunction`\<`GetDisabledServicesResult`\<`void`\>\> | Returns a list of disabled services |

## Returns

`void`
