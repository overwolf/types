---
sidebar_label: "youtube.share"
---

Overwolf APIs / overwolf.benchmarking / overwolf / social / youtube / share

```ts
function share(
  youTubeShareParams: ShareParameters,
  callback: CallbackFunction<Result>,
): void;
```

If the user is currently logged into YouTube, this will perform the video
share.

Possible errors that can occur:

- Disconnected (user isn't signed in)
- MissingFile (trying to share a missing file)
- UnsupportedFile (trying to share an unsupported format)

## Parameters

| Parameter            | Type                                                                                                                                            | Description                             |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- |
| `youTubeShareParams` | `ShareParameters`                                                                                          | The share parameters.                   |
| `callback`           | `CallbackFunction`\<`Result`\> | Will contain the status of the request. |

## Returns

`void`
