Overwolf APIs / overwolf.benchmarking / overwolf / utils / sendKeyStroke

```ts
function sendKeyStroke(keyString: string): void;
```

Sends a string representing a key stroke to the game, causing a simulated
key stroke.

## Parameters

| Parameter   | Type     | Description                                                   |
| ----------- | -------- | ------------------------------------------------------------- |
| `keyString` | `string` | The key or key combination to send, as a string. e.g. "Alt+I" |

## Returns

`void`
