---
sidebar_label: "openUrlInDefaultBrowser"
---

Overwolf APIs / overwolf / overwolf / utils / openUrlInDefaultBrowser

```ts
function openUrlInDefaultBrowser(url: string): void;
```

Opens the url in the user's default browser.

## Parameters

| Parameter | Type     | Description    |
| --------- | -------- | -------------- |
| `url`     | `string` | A url to open. |

## Returns

`void`
