---
sidebar_label: "openUrlInOverwolfBrowser"
---

Overwolf APIs / overwolf.benchmarking / overwolf / utils / openUrlInOverwolfBrowser

```ts
function openUrlInOverwolfBrowser(url: string, targetTabName?: string): void;
```

Opens the url in Overwolf's browser.

## Parameters

| Parameter        | Type     | Description    |
| ---------------- | -------- | -------------- |
| `url`            | `string` | A url to open. |
| `targetTabName?` | `string` | -              |

## Returns

`void`
