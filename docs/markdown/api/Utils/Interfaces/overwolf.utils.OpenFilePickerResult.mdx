---
sidebar_label: "OpenFilePickerResult"
---

Overwolf APIs / overwolf / overwolf / utils / OpenFilePickerResult

Result returned from a file picker dialog.

## Extends

- `Result`

## Properties

| Property                                | Type       | Description                                                  |
| --------------------------------------- | ---------- | ------------------------------------------------------------ |
| <a id="property-error"></a> `error?`    | `string`   | Information regarding the error (if an error occurred)       |
| <a id="property-file"></a> `file?`      | `string`   | File system path of the selected file (single selection).    |
| <a id="property-files"></a> `files?`    | `string`[] | File system paths of the selected files (multi-selection).   |
| <a id="property-success"></a> `success` | `boolean`  | Whether the method executed successfully or not.             |
| <a id="property-url"></a> `url?`        | `string`   | Overwolf media URL of the selected file (single selection).  |
| <a id="property-urls"></a> `urls?`      | `string`[] | Overwolf media URLs of the selected files (multi-selection). |
