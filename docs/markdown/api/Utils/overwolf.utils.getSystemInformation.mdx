Overwolf APIs / overwolf.benchmarking / overwolf / utils / getSystemInformation

```ts
function getSystemInformation(
  callback: (result: GetSystemInformationResult) => void,
): void;
```

Returns system information which includes information about CPU, Monitors,
GPU, HDD, RAM and more.

## Parameters

| Parameter  | Type                                                                                               | Description                         |
| ---------- | -------------------------------------------------------------------------------------------------- | ----------------------------------- |
| `callback` | (`result`: `GetSystemInformationResult`) =&gt; `void` | Called with the system information. |

## Returns

`void`
