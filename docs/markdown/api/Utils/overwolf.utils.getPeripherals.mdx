Overwolf APIs / overwolf.benchmarking / overwolf / utils / getPeripherals

```ts
function getPeripherals(callback: () => void): void;
```

Returns system Peripherals information.

## Parameters

| Parameter  | Type         | Description                         |
| ---------- | ------------ | ----------------------------------- |
| `callback` | () =&gt; `void` | Called with the system information. |

## Returns

`void`
