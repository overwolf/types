Overwolf APIs / overwolf.cryptography / stopWatchingFile

```ts
function stopWatchingFile(
  path: string,
  callback: CallbackFunction<Result>,
): void;
```

## Parameters

| Parameter  | Type                                                                                  |
| ---------- | ------------------------------------------------------------------------------------- |
| `path`     | `string`                                                                              |
| `callback` | `CallbackFunction`\&lt;`Result`\&gt; |

## Returns

`void`
